namespace gpkih::help::reset
{
	const char *usage = R"(== reset ==
reset gpkih, removing every profile and gpkih source dir, where database and logs are located

[ syntax ]
./gpkih reset
)";
	const char *examples = R"()";
}