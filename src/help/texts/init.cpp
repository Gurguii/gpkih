namespace gpkih::help::init
{
	const char *usage = R"(== init ==
Create a new profile with its according PKI file structure

[ syntax ]
  ./gpki init [subopts]

[ subopts ]
  -n | --name : profile name
  -s | --source : source dir (where profile related files will be kept
)";
	const char *examples = R"()";
}