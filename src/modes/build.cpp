#include "build.hpp"

using namespace gpki;

int modes::build::ca() { 
    std::cout << "build ca function being called\n";
    return 0; }
int modes::build::server() { return 0; }
int modes::build::client() { return 0; }
