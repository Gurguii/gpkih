#include <string>
#include <vector>
namespace gpki::subparser {
int build(std::vector<std::string> opts) { return 0; };
int profile(std::vector<std::string> opts) { return 0; };
int entity(std::vector<std::string> opts) { return 0; };
} // namespace gpki::subparser
